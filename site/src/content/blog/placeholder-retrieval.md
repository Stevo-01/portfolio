---
title: 'Retrieval is cheaper than parameters'
description: 'PLACEHOLDER fixture. Exists so collection queries return something during the UI slices. Slice 07 replaces it with real writing.'
category: llms-and-rag
publishDate: 2026-08-20
draft: false
tags: ['rag', 'retrieval', 'evaluation']
heroGlyph: sparkles
---

**PLACEHOLDER.** This file exists so that `getPublishedPosts()` returns a
non-empty list while the layout, journal and article slices are being built.
Slice 07 deletes it.

## A heading, so the TOC has something to render

Body text. The article template needs a document with real structure to render
against, but that fixture is slice 07's job — this one only has to parse.

### A nested heading

Enough depth to confirm heading levels are not skipped.
