# Feeds /blog/category/machine-learning/, the CategoryTabs strip and the
# discipline grid on /blog/. Article counts are derived at build time.
slug: machine-learning
title: "Machine Learning"
description: "Training, evaluation, and the parts of a model's behaviour that only show up outside the validation set."
order: 1
